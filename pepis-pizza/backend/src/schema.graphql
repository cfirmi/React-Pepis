# import * from './generated/prisma.graphql'

type User {
    id: ID!
    name: String
}

type Mutation {
    createproductItem(title: String, description: String, price: Int, category: String, image: String, largeImage: String): productItem!
    # createCategory(title: String, subCategory: String ): Category!
    # createsubCategory(title: String): subCategory!
}

type Query {
    productItems: [productItem]!
    # categories: [Category]!
    # subCategories: [subCategory]!
}